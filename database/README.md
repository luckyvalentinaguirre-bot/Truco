# TRUCO · Base de datos (pendiente — Etapa 2+)

Todavía sin implementar. Reservado para el esquema y las migraciones de
**PostgreSQL**.

Entidades previstas (alineadas con `frontend/src/types/domain.ts`):

- `users` — cuenta, credenciales, nivel/XP.
- `profiles` — avatar, rango, MMR.
- `stats` — victorias, derrotas, winrate, trucos/envidos/flores, rachas.
- `matches` / `match_players` — historial de partidas y resultados.
- `friends` — relaciones sociales.
- `seasons`, `rankings` — competitivo por temporadas (futuro).
- `cosmetics`, `inventory` — cosméticos (futuro, no pay-to-win).

Ver `../docs/ARCHITECTURE.md`.
