# =============================================================
# Frontend · Variables de entorno (EJEMPLO — sin valores reales)
# Copiá como `.env` o `.env.local`. No commitear el real.
# =============================================================

# URL base del backend. Local: http://localhost:10000
# En producción, la URL del servicio de Render.
VITE_API_URL=http://localhost:10000
